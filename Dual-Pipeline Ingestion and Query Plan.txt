Here  is  a  detailed,  step-by-step  implementation  plan  for  building  the  dual-pipeline  architecture  
(SimpleMem
 
for
 
prompts,
 
PageIndex
 
for
 
documents)
 
in
 
your
 
CortexDB
 
memory
 
Spring
 
Boot
 
backend.
 
Phase  1:  API  and  DTO  Layer  Modifications  
First,  we  need  to  create  distinct  entry  points  for  your  different  data  types.  
Step  1:  Update  DTOs  (Data  Transfer  Objects)  In  memory/src/main/java/com/vectornode/memory/ingest/dto/request/,  separate  your  ingestion  
requests.
 ●  Keep/Modify  IngestContentRequest.java:  Rename  this  to  IngestPromptRequest.java.  It  
should
 
contain
 
String
 
text
 
(the
 
user's
 
chat
 
message)
 
and
 
String
 
knowledgeBaseId.
 ●  Create  IngestDocumentRequest.java:  This  should  handle  larger  payloads.  It  should  
contain
 
String
 
documentText
 
(or
 
a
 
MultipartFile
 
if
 
you
 
plan
 
to
 
parse
 
PDFs
 
directly
 
in
 
Java
 
later),
 
String
 
documentTitle,
 
and
 
String
 
knowledgeBaseId.
 
Step  2:  Update  IngestController.java  
In  memory/src/main/java/com/vectornode/memory/ingest/controller/IngestController.java,  
create
 
two
 
distinct
 
endpoints:
 
 
Java  
  @PostMapping("/prompt") public ResponseEntity<IngestResponse>  ingestPrompt(@RequestBody IngestPromptRequest  request)  {  
    //  Routes  to  SimpleMem  pipeline 
    return ResponseEntity.ok(ingestService.processPrompt(request));  
}
 
 @PostMapping("/document") public ResponseEntity<IngestResponse>  ingestDocument(@RequestBody IngestDocumentRequest  
request)
 {  
    //  Routes  to  PageIndex  pipeline 
    return ResponseEntity.ok(ingestService.processDocument(request));  
}
 
 
Phase  2:  The  Document  Pipeline  (PageIndex  Logic)  
This  pipeline  focuses  on  extracting  hierarchical  structure  from  large  texts.  
Step  1:  Create  PageIndexService.java  Create  a  new  service  in  your  ingest/service/  package.  ●  The  LLM  Call:  Write  a  method  generateDocumentTree(String  documentText).  This  method  
formats
 
a
 
prompt
 
to
 
your
 
LLMProvider
 
instructing
 
it
 
to
 
analyze
 
the
 
document
 
and
 
output
 
a
 
nested
 
JSON
 
structure
 
(Table
 
of
 
Contents)
 
containing
 
section
 
titles,
 
brief
 
summaries,
 
and
 
the
 
actual
 
content
 
of
 
that
 
section.
 ●  JSON  Parsing:  Use  Jackson  (ObjectMapper)  to  parse  the  LLM's  JSON  response  into  a  
custom
 
Java
 
POJO
 
(e.g.,
 
DocumentNode.java
 
containing
 
title,
 
summary,
 
content,
 
and
 
List<DocumentNode>
 
children).
 
Step  2:  Map  the  Tree  to  PostgreSQL  via  IngestService.java  In  your  processDocument  method:  1.  Call  pageIndexService.generateDocumentTree().  2.  Traverse  the  returned  tree  recursively.  3.  For  each  node:  Save  it  as  a  Context  entity.  Set  text_chunk  to  the  section's  content.  Save  
the
 
title
 
and
 
summary
 
into
 
the
 
metadata
 
JSONB
 
column.
 4.  Graph  Construction:  For  every  parent-child  relationship  in  the  tree,  create  a  Relation  
entity
 
(e.g.,
 
source
 
=
 
Parent
 
Context
 
ID,
 
target
 
=
 
Child
 
Context
 
ID,
 
type
 
=
 
"HAS_SUBSECTION").
 
Save
 
this
 
using
 
RelationRepository.
 
Phase  3:  The  Prompt  Pipeline  (SimpleMem  Logic)  
This  pipeline  focuses  on  compressing  and  merging  episodic  chat  memory.  
Step  1:  Upgrade  ChunkingService.java  Transform  this  from  a  naive  text  splitter  into  a  semantic  compressor.  ●  The  LLM  Call:  Update  chunkText()  to  call  the  LLMProvider.  Instruct  the  LLM  to  take  the  raw  
prompt
 
and
 
rewrite
 
it
 
into
 
a
 
lossless_restatement—a
 
standalone
 
fact
 
with
 
coreferences
 
resolved
 
(e.g.,
 
resolving
 
"he"
 
to
 
the
 
actual
 
entity
 
name)
 
and
 
explicit
 
timestamps.
 ●  Have  the  LLM  also  output  a  list  of  core  keywords.  
Step  2:  Implement  Online  Synthesis  in  IngestService.java  In  your  processPrompt  method:  1.  Call  the  upgraded  ChunkingService  to  get  the  compressed  fact  and  keywords.  2.  Similarity  Check:  Use  ContextRepository  to  perform  a  vector  search  against  the  
database
 
for
 
the
 
new
 
fact's
 
embedding.
 
If
 
a
 
highly
 
similar
 
Context
 
exists
 
(e.g.,
 
>
 
0.85
 
similarity):
 3.  Merge  (Synthesis):  Send  the  existing  Context  text  and  the  new  fact  to  the  LLM  to  write  a  
merged
 
summary.
 
Use
 
ContextRepository.save()
 
to
 
overwrite
 
the
 
existing
 
row
 
with
 
the
 
merged
 
text.
 4.  Insert  (No  Synthesis):  If  no  similar  context  exists,  insert  it  as  a  new  Context  row,  storing  
the  extracted  keywords  in  the  metadata  JSONB  column.  
Phase  4:  The  Agentic  Router  (Query  Phase)  
Now  that  the  database  holds  two  different  types  of  memory  (Document  Trees  and  Prompt  
Facts),
 
your
 
query
 
engine
 
needs
 
to
 
be
 
smart
 
enough
 
to
 
know
 
which
 
to
 
use.
 
Step  1:  Build  the  Router  in  QueryService.java  In  memory/src/main/java/com/vectornode/memory/query/service/QueryService.java,  intercept  
the
 
incoming
 
QueryRequest.
 ●  The  Routing  LLM  Call:  Before  doing  any  database  lookups,  send  the  user's  query  to  a  fast,  
cheap
 
LLM
 
model
 
(like
 
Llama
 
3
 
8B
 
or
 
GPT-4o-mini).
 ●  Prompt:  "The  user  asked:  '[Query]'.  Does  this  question  require  looking  up  their  personal  
conversational
 
history
 
(prompts),
 
or
 
looking
 
up
 
reference
 
documents?"
 ●  Parse  the  output  to  an  enum:  MemoryType.PROMPT  or  MemoryType.DOCUMENT.  
Step  2:  Execute  the  Routed  Search  ●  If  MemoryType.PROMPT:  Execute  the  SimpleMem  Hybrid  Search .  Combine  a  vector  
similarity
 
search
 
(ContextRepository)
 
with
 
a
 
keyword
 
filter
 
against
 
the
 
metadata
 
JSONB
 
column.
 ●  If  MemoryType.DOCUMENT:  Execute  the  PageIndex  Agentic  Traversal .  1.  Query  the  database  for  all  root-level  document  nodes  (Contexts  with  no  parent  
relations).
 2.  Send  their  titles/summaries  to  the  LLM  to  pick  the  most  relevant  branch.  3.  Use  RelationRepository  to  fetch  the  children  of  the  chosen  branch.  4.  Repeat  until  you  reach  the  leaf  node  with  the  exact  answer.  
Step  3:  Final  Generation  
Pass  the  successfully  retrieved  information  (either  the  merged  conversational  facts  or  the  
specific
 
document
 
subsection)
 
to
 
the
 
LLMProvider
 
to
 
generate
 
the
 
final
 
QueryResponse
 
to
 
the
 
user.
 
